# Telegram ingress-stub (paper) — Fastigheterutomlands

Ingen live-skick. Token från BotFather hör hemma i lokal tenant-config, aldrig i git.

```bash
cd marknad/telegram/ingress-stub
cp config.example.json config.json
npm start
npm run test:all
```

Port default `8788`. Samma `intake.json` som WhatsApp-paper.
